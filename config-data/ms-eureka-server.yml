server:
  port: '8761'
eureka:
  client:
    fetch-registry: 'false'
    register-with-eureka: 'false'
    service-url:
      defaultZone: ${eureka.instance.hostname}:${server.port}
      #defaultZone: ${eureka.instance.hostname}:${server.port}/eureka/